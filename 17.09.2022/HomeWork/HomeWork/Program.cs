using HomeWork.Extentions;
using HomeWork.Models;
using System;

namespace HomeWork
{
    class Program
    {
        static void Main(string[] args)
        {
            #region HomeWork
            /*            Console.WriteLine("Enter FullName: ");
                        string fullname = Console.ReadLine();

                        Console.WriteLine("Enter Group No: ");
                        string groupNo = Console.ReadLine();

                        Console.WriteLine("Enter Age:");
                        byte age = Convert.ToByte(Console.ReadLine());

                        Student student = new Student(fullname,groupNo,age);

                        Console.WriteLine($"FullName: {student.FullName}\nGroupNo: {student.GroupNo}\nAge: {student.Age}");*/
            #endregion

            #region Task 1
            /*
                        int a = 5;
                        string b = "salam";
                        int[] arr = { 12, 345, 456, 67,12 };
                        char chr = 'a';


                        Console.WriteLine(a.IsEven());
                        Console.WriteLine(a.IsOdd());
                        Console.WriteLine(b.IsContainsDigit());
                        foreach (var item in b.GetValueIndexesStr(chr))
                        {
                            Console.WriteLine(item);
                        }
                        foreach (var item in arr.GetValueIndexes(12))
                        {
                            Console.WriteLine(item);
                        }*/
            #endregion

        }
    }
}
